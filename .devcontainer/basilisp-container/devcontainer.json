{
	"name": "dual-basilisp",
	"image": "mcr.microsoft.com/devcontainers/base:noble",
	"workspaceMount": "source=${localWorkspaceFolder}/dual_basilisp,target=/workspace,type=bind,consistency=cached", 
	"workspaceFolder": "/workspace",
	"features": {
		"ghcr.io/eitsupi/devcontainer-features/jq-likes:2": {},
		"ghcr.io/va-h/devcontainers-features/uv:1": {}
	},
		"runArgs": ["--network", "dual-network",
	            "--name", "dual-basilisp" ],


	"onCreateCommand": "uv venv --allow-existing",
	"postCreateCommand": "uv sync && echo 'source .venv/bin/activate' >> ~/.bashrc && echo 'source .venv/bin/activate' >> ~/.profile",
	"customizations": {
		"vscode": {
			"extensions": [
				"betterthantomorrow.calva"
			]
		}
	}

}
